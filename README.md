required libraries: pandas pyodbc

### download library via
in the root of the project
```
pip install -r requirements.txt
```
then put csv file into the /input folder

in the root run 
```
python main.py --import
```
